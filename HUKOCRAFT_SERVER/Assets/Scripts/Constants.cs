
public class Constants
{
    //HOW MANY TICKS (PER SECOND)
    public const int TICKS_PER_SEC = 30; 
    //HOW MANY TICKS (IN MILLISECOND) (PER SECOND)
    public const float MS_PER_TICK = 1000f / TICKS_PER_SEC; 
}
